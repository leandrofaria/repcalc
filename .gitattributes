# Normalize line endings to LF in the repository, regardless of the OS
# the commit was made on. Checkouts still use the platform's convention.
* text=auto eol=lf

# Binary assets must never be touched by line-ending conversion.
*.png binary
*.jpg binary
*.ico binary
*.webp binary
